---
import Layout from '../layouts/Layout.astro';
import { copy } from '../lib/site';

const t = copy.es;
---

<Layout title={`${t.notFoundTitle} · Notofilia`} description={t.notFoundBody} locale="es" pathname="/404">
  <main id="main-content" tabindex="-1" class="mx-auto flex min-h-[60vh] max-w-[720px] flex-col justify-center px-[var(--page-gutter)] py-24 outline-none">
    <h1 class="mt-0 mb-4 font-display text-[2rem]/10 font-normal text-brand-900">{t.notFoundTitle}</h1>
    <p class="m-0 text-[17px]/7 text-ink-600">{t.notFoundBody}</p>
    <p class="mt-8">
      <a
        href="/"
        class="inline-flex min-h-11 items-center font-sans text-[15px] text-brand-800 hover:text-brand-600"
      >
        {t.backHome}
      </a>
    </p>
  </main>
</Layout>
