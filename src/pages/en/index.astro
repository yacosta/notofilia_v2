---
import Layout from '../../layouts/Layout.astro';
import HomePage from '../../components/HomePage.astro';
import { copy } from '../../lib/site';

const t = copy.en;
---

<Layout title={t.metaTitle} description={t.metaDescription} locale="en" pathname="/en/">
  <HomePage locale="en" />
</Layout>
