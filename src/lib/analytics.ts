/** Google Analytics (GA4). Measurement IDs are public; they ship in every page's HTML. */
export const GA_MEASUREMENT_ID = 'G-M5T95FWH2Z';

/** localStorage key holding the visitor's analytics-cookie choice ('granted' | 'denied'). */
export const CONSENT_STORAGE_KEY = 'notofilia-analytics-consent';
